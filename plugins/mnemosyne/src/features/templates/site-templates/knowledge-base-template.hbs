{{!-- 
  Knowledge Base Template
  Advanced template with interactive knowledge graph and comprehensive navigation
--}}
<!DOCTYPE html>
<html lang="{{site.language}}" class="knowledge-base-theme">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{#if page.title}}{{page.title}} - {{/if}}{{site.title}}</title>
  
  {{!-- SEO and Meta Tags --}}
  {{#if document.metadata.description}}
  <meta name="description" content="{{document.metadata.description}}">
  {{/if}}
  {{#if document.metadata.keywords}}
  <meta name="keywords" content="{{join document.metadata.keywords ', '}}">
  {{/if}}
  
  {{!-- Stylesheets --}}
  <link rel="stylesheet" href="{{site.baseUrl}}assets/css/main.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  
  {{!-- Knowledge Graph Dependencies --}}
  {{#if options.includeKnowledgeGraph}}
  <script src="https://d3js.org/d3.v7.min.js"></script>
  {{/if}}
  
  <style>
    /* Knowledge Base Theme Styles */
    :root {
      --primary-color: #6366f1;
      --secondary-color: #8b5cf6;
      --accent-color: #10b981;
      --background-color: #ffffff;
      --surface-color: #f8fafc;
      --sidebar-color: #1e293b;
      --text-color: #0f172a;
      --muted-color: #64748b;
      --border-color: #e2e8f0;
      --hover-color: #f1f5f9;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      line-height: 1.6;
      color: var(--text-color);
      background-color: var(--background-color);
      display: grid;
      grid-template-columns: 320px 1fr;
      grid-template-rows: auto 1fr;
      grid-template-areas: 
        "sidebar header"
        "sidebar main";
      min-height: 100vh;
    }
    
    /* Header */
    .site-header {
      grid-area: header;
      background: var(--surface-color);
      border-bottom: 1px solid var(--border-color);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    
    .page-title {
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--text-color);
    }
    
    .header-actions {
      display: flex;
      gap: 1rem;
      align-items: center;
    }
    
    .search-toggle,
    .graph-toggle {
      background: none;
      border: 1px solid var(--border-color);
      color: var(--text-color);
      padding: 0.5rem;
      border-radius: 0.5rem;
      cursor: pointer;
      transition: all 0.2s;
    }
    
    .search-toggle:hover,
    .graph-toggle:hover {
      background: var(--hover-color);
      border-color: var(--primary-color);
    }
    
    /* Sidebar */
    .sidebar {
      grid-area: sidebar;
      background: var(--sidebar-color);
      color: white;
      overflow-y: auto;
      padding: 1.5rem;
    }
    
    .sidebar-brand {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      margin-bottom: 1.5rem;
    }
    
    .brand-logo {
      width: 32px;
      height: 32px;
      background: var(--primary-color);
      border-radius: 0.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      color: white;
    }
    
    .brand-title {
      font-size: 1.25rem;
      font-weight: 600;
    }
    
    .sidebar-nav {
      margin-bottom: 2rem;
    }
    
    .nav-section {
      margin-bottom: 1.5rem;
    }
    
    .nav-section-title {
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.6);
      margin-bottom: 0.75rem;
      letter-spacing: 0.05em;
    }
    
    .nav-list {
      list-style: none;
    }
    
    .nav-item {
      margin-bottom: 0.25rem;
    }
    
    .nav-link {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.5rem 0.75rem;
      color: rgba(255, 255, 255, 0.8);
      text-decoration: none;
      border-radius: 0.375rem;
      transition: all 0.2s;
      font-size: 0.875rem;
    }
    
    .nav-link:hover,
    .nav-link.active {
      background: rgba(255, 255, 255, 0.1);
      color: white;
    }
    
    .nav-icon {
      width: 16px;
      height: 16px;
      opacity: 0.7;
    }
    
    /* Recent Pages */
    .recent-pages {
      margin-top: 2rem;
    }
    
    .recent-item {
      padding: 0.5rem 0.75rem;
      margin-bottom: 0.25rem;
      border-radius: 0.375rem;
      transition: background 0.2s;
    }
    
    .recent-item:hover {
      background: rgba(255, 255, 255, 0.05);
    }
    
    .recent-link {
      color: rgba(255, 255, 255, 0.7);
      text-decoration: none;
      font-size: 0.875rem;
      display: block;
    }
    
    .recent-meta {
      font-size: 0.75rem;
      color: rgba(255, 255, 255, 0.5);
      margin-top: 0.25rem;
    }
    
    /* Main Content */
    .main-content {
      grid-area: main;
      padding: 2rem;
      overflow-y: auto;
      max-width: none;
    }
    
    /* Breadcrumbs */
    .breadcrumbs {
      background: var(--surface-color);
      border: 1px solid var(--border-color);
      border-radius: 0.5rem;
      padding: 0.75rem 1rem;
      margin-bottom: 2rem;
    }
    
    .breadcrumb-list {
      list-style: none;
      display: flex;
      gap: 0.5rem;
      align-items: center;
    }
    
    .breadcrumb-item a {
      color: var(--primary-color);
      text-decoration: none;
    }
    
    .breadcrumb-item:not(:last-child)::after {
      content: '›';
      margin-left: 0.5rem;
      color: var(--muted-color);
    }
    
    /* Article */
    .article-header {
      margin-bottom: 2rem;
    }
    
    .article-title {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--text-color);
      line-height: 1.2;
    }
    
    .article-meta {
      display: flex;
      gap: 2rem;
      flex-wrap: wrap;
      color: var(--muted-color);
      font-size: 0.875rem;
      margin-bottom: 1.5rem;
    }
    
    .meta-item {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .article-tags {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
      margin-top: 1rem;
    }
    
    .tag {
      background: var(--surface-color);
      color: var(--text-color);
      padding: 0.375rem 0.75rem;
      border-radius: 1.5rem;
      font-size: 0.75rem;
      font-weight: 500;
      text-decoration: none;
      border: 1px solid var(--border-color);
      transition: all 0.2s;
    }
    
    .tag:hover {
      background: var(--primary-color);
      color: white;
      border-color: var(--primary-color);
      transform: translateY(-1px);
    }
    
    /* Table of Contents */
    .table-of-contents {
      background: var(--surface-color);
      border: 1px solid var(--border-color);
      border-radius: 0.75rem;
      padding: 1.5rem;
      margin-bottom: 2rem;
      position: sticky;
      top: 6rem;
      max-height: calc(100vh - 8rem);
      overflow-y: auto;
    }
    
    .toc-title {
      font-size: 1.125rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: var(--text-color);
    }
    
    .toc-list {
      list-style: none;
    }
    
    .toc-item {
      margin: 0.25rem 0;
    }
    
    .toc-item a {
      display: block;
      padding: 0.5rem 0.75rem;
      color: var(--muted-color);
      text-decoration: none;
      border-radius: 0.375rem;
      transition: all 0.2s;
      font-size: 0.875rem;
    }
    
    .toc-item a:hover {
      background: var(--hover-color);
      color: var(--primary-color);
    }
    
    .toc-level-1 { margin-left: 0; font-weight: 500; }
    .toc-level-2 { margin-left: 1rem; }
    .toc-level-3 { margin-left: 2rem; }
    .toc-level-4 { margin-left: 3rem; }
    
    /* Article Content */
    .content-wrapper {
      display: grid;
      grid-template-columns: 1fr 280px;
      gap: 2rem;
      align-items: start;
    }
    
    .article-content {
      max-width: none;
      line-height: 1.7;
    }
    
    .article-content h1,
    .article-content h2,
    .article-content h3,
    .article-content h4,
    .article-content h5,
    .article-content h6 {
      color: var(--text-color);
      margin-top: 2rem;
      margin-bottom: 1rem;
      line-height: 1.3;
      position: relative;
    }
    
    .article-content h1 { font-size: 2rem; font-weight: 700; }
    .article-content h2 { font-size: 1.5rem; font-weight: 600; }
    .article-content h3 { font-size: 1.25rem; font-weight: 600; }
    .article-content h4 { font-size: 1.125rem; font-weight: 500; }
    
    .article-content .anchor-link {
      position: absolute;
      left: -1.5rem;
      opacity: 0;
      transition: opacity 0.2s;
      color: var(--muted-color);
      text-decoration: none;
    }
    
    .article-content h1:hover .anchor-link,
    .article-content h2:hover .anchor-link,
    .article-content h3:hover .anchor-link {
      opacity: 1;
    }
    
    .article-content p {
      margin-bottom: 1.5rem;
    }
    
    .article-content a {
      color: var(--primary-color);
      text-decoration: none;
      border-bottom: 1px solid transparent;
      transition: border-color 0.2s;
    }
    
    .article-content a:hover {
      border-bottom-color: var(--primary-color);
    }
    
    .article-content .internal-link {
      background: linear-gradient(135deg, var(--primary-color)20, var(--secondary-color)20);
      padding: 0.125rem 0.25rem;
      border-radius: 0.25rem;
      border-bottom: none;
    }
    
    .article-content blockquote {
      border-left: 4px solid var(--primary-color);
      margin: 2rem 0;
      padding: 1rem 0 1rem 1.5rem;
      background: var(--surface-color);
      border-radius: 0 0.5rem 0.5rem 0;
      font-style: italic;
    }
    
    .article-content pre {
      background: var(--sidebar-color);
      color: #e2e8f0;
      border-radius: 0.75rem;
      padding: 1.5rem;
      margin: 2rem 0;
      overflow-x: auto;
      font-family: 'JetBrains Mono', monospace;
    }
    
    .article-content code {
      background: var(--surface-color);
      color: var(--primary-color);
      padding: 0.25rem 0.5rem;
      border-radius: 0.375rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.875em;
    }
    
    .article-content pre code {
      background: none;
      color: inherit;
      padding: 0;
    }
    
    /* Knowledge Graph Preview */
    .knowledge-graph-preview {
      background: var(--surface-color);
      border: 1px solid var(--border-color);
      border-radius: 0.75rem;
      padding: 1.5rem;
      margin-bottom: 2rem;
    }
    
    .graph-preview-title {
      font-size: 1.125rem;
      font-weight: 600;
      margin-bottom: 1rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    
    .view-full-graph {
      font-size: 0.875rem;
      color: var(--primary-color);
      text-decoration: none;
    }
    
    .mini-graph {
      height: 200px;
      border: 1px solid var(--border-color);
      border-radius: 0.5rem;
      background: white;
      position: relative;
      overflow: hidden;
    }
    
    /* Related Content */
    .related-content {
      margin-top: 3rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border-color);
    }
    
    .related-title {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
    }
    
    .related-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1rem;
    }
    
    .related-card {
      background: var(--surface-color);
      border: 1px solid var(--border-color);
      border-radius: 0.75rem;
      padding: 1.5rem;
      transition: all 0.2s;
    }
    
    .related-card:hover {
      border-color: var(--primary-color);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(99, 102, 241, 0.15);
    }
    
    .related-card h3 {
      margin: 0 0 0.5rem 0;
      font-size: 1.125rem;
    }
    
    .related-card a {
      color: var(--text-color);
      text-decoration: none;
    }
    
    .related-description {
      color: var(--muted-color);
      font-size: 0.875rem;
      line-height: 1.5;
    }
    
    /* Backlinks */
    .backlinks {
      margin-top: 3rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border-color);
    }
    
    .backlinks-title {
      font-size: 1.125rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: var(--text-color);
    }
    
    .backlink-list {
      list-style: none;
    }
    
    .backlink-item {
      padding: 1rem;
      background: var(--surface-color);
      border: 1px solid var(--border-color);
      border-radius: 0.5rem;
      margin-bottom: 0.75rem;
      transition: all 0.2s;
    }
    
    .backlink-item:hover {
      border-color: var(--primary-color);
    }
    
    .backlink-title {
      font-weight: 500;
      margin-bottom: 0.5rem;
    }
    
    .backlink-context {
      font-size: 0.875rem;
      color: var(--muted-color);
      line-height: 1.5;
    }
    
    /* Search Modal */
    .search-modal {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.5);
      display: none;
      align-items: flex-start;
      justify-content: center;
      padding-top: 10vh;
      z-index: 1000;
    }
    
    .search-modal.active {
      display: flex;
    }
    
    .search-modal-content {
      background: white;
      border-radius: 1rem;
      width: 90%;
      max-width: 600px;
      max-height: 80vh;
      overflow: hidden;
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
    }
    
    .search-header {
      padding: 1.5rem;
      border-bottom: 1px solid var(--border-color);
      display: flex;
      gap: 1rem;
    }
    
    .search-input {
      flex: 1;
      border: 1px solid var(--border-color);
      border-radius: 0.5rem;
      padding: 0.75rem 1rem;
      font-size: 1rem;
      outline: none;
    }
    
    .search-input:focus {
      border-color: var(--primary-color);
      box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
    }
    
    .search-close {
      background: none;
      border: none;
      color: var(--muted-color);
      cursor: pointer;
      padding: 0.5rem;
    }
    
    .search-results {
      padding: 1rem;
      max-height: 50vh;
      overflow-y: auto;
    }
    
    .search-result {
      padding: 1rem;
      border-radius: 0.5rem;
      margin-bottom: 0.5rem;
      cursor: pointer;
      transition: background 0.2s;
    }
    
    .search-result:hover {
      background: var(--surface-color);
    }
    
    .search-result-title {
      font-weight: 500;
      margin-bottom: 0.25rem;
    }
    
    .search-result-excerpt {
      font-size: 0.875rem;
      color: var(--muted-color);
      line-height: 1.4;
    }
    
    /* Responsive Design */
    @media (max-width: 1024px) {
      body {
        grid-template-columns: 1fr;
        grid-template-areas: 
          "header"
          "main";
      }
      
      .sidebar {
        display: none;
      }
      
      .content-wrapper {
        grid-template-columns: 1fr;
      }
      
      .table-of-contents {
        position: static;
        max-height: none;
      }
    }
    
    @media (max-width: 768px) {
      .main-content {
        padding: 1rem;
      }
      
      .article-title {
        font-size: 2rem;
      }
      
      .related-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="site-header">
    <h1 class="page-title">{{page.title}}</h1>
    <div class="header-actions">
      {{#if options.generateSearch}}
      <button class="search-toggle" aria-label="Search">
        <svg width="20" height="20" fill="currentColor" viewBox="0 0 24 24">
          <path d="M21.53 20.47l-3.66-3.66C19.195 15.24 20 13.214 20 11c0-4.97-4.03-9-9-9s-9 4.03-9 9 4.03 9 9 9c2.215 0 4.24-.804 5.808-2.13l3.66 3.66c.147.146.34.22.53.22s.385-.073.53-.22c.295-.293.295-.767.002-1.06zM3.5 11c0-4.135 3.365-7.5 7.5-7.5s7.5 3.365 7.5 7.5-3.365 7.5-7.5 7.5-7.5-3.365-7.5-7.5z"/>
        </svg>
      </button>
      {{/if}}
      
      {{#if options.includeKnowledgeGraph}}
      <button class="graph-toggle" aria-label="Knowledge Graph">
        <svg width="20" height="20" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
        </svg>
      </button>
      {{/if}}
    </div>
  </header>

  <!-- Sidebar -->
  <aside class="sidebar">
    <div class="sidebar-brand">
      <div class="brand-logo">{{substr site.title 0 1}}</div>
      <div class="brand-title">{{site.title}}</div>
    </div>
    
    <nav class="sidebar-nav">
      <div class="nav-section">
        <div class="nav-section-title">Navigation</div>
        <ul class="nav-list">
          {{#each navigation}}
          <li class="nav-item">
            <a href="{{path}}" class="nav-link {{#if current}}active{{/if}}">
              <span class="nav-icon">📄</span>
              {{title}}
            </a>
          </li>
          {{/each}}
        </ul>
      </div>
      
      {{#if options.includeTagCloud}}
      <div class="nav-section">
        <div class="nav-section-title">Popular Tags</div>
        <ul class="nav-list">
          {{#each (take site.popularTags 8)}}
          <li class="nav-item">
            <a href="{{../site.baseUrl}}tags/{{slugify name}}/" class="nav-link">
              <span class="nav-icon">#</span>
              {{name}} ({{count}})
            </a>
          </li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
    </nav>
    
    {{#if site.recentPages}}
    <div class="recent-pages">
      <div class="nav-section-title">Recent Pages</div>
      {{#each (take site.recentPages 6)}}
      <div class="recent-item">
        <a href="{{path}}.html" class="recent-link">{{title}}</a>
        <div class="recent-meta">{{formatDate modified "MMM d"}}</div>
      </div>
      {{/each}}
    </div>
    {{/if}}
  </aside>

  <!-- Main Content -->
  <main class="main-content">
    {{!-- Breadcrumbs --}}
    {{#if options.includeBreadcrumbs}}
    {{#if page.breadcrumbs}}
    <nav class="breadcrumbs" aria-label="Breadcrumb">
      <ol class="breadcrumb-list">
        {{#each page.breadcrumbs}}
        <li class="breadcrumb-item">
          {{#if current}}
          <span>{{title}}</span>
          {{else}}
          <a href="{{path}}">{{title}}</a>
          {{/if}}
        </li>
        {{/each}}
      </ol>
    </nav>
    {{/if}}
    {{/if}}

    <div class="content-wrapper">
      <div class="article-container">
        <!-- Article Header -->
        <header class="article-header">
          <h1 class="article-title">{{document.title}}</h1>
          
          <div class="article-meta">
            {{#if document.metadata.author}}
            <div class="meta-item">
              <svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
              </svg>
              {{document.metadata.author}}
            </div>
            {{/if}}
            
            {{#if document.metadata.created}}
            <div class="meta-item">
              <svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
                <path d="M19 3h-1V1h-2v2H8V1H6v2H5c-1.11 0-1.99.9-1.99 2L3 19c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H5V8h14v11zM7 10h5v5H7z"/>
              </svg>
              {{formatDate document.metadata.created "MMMM d, yyyy"}}
            </div>
            {{/if}}
            
            {{#if document.readingTime}}
            <div class="meta-item">
              <svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
              </svg>
              {{document.readingTime}} min read
            </div>
            {{/if}}
            
            {{#if document.wordCount}}
            <div class="meta-item">
              <svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
                <path d="M14 2H6c-1.1 0-1.99.9-1.99 2L4 20c0 1.1.89 2 2 2h12c1.1 0 2-.9 2-2V8l-6-6zm2 16H8v-2h8v2zm0-4H8v-2h8v2zm-3-5V3.5L18.5 9H13z"/>
              </svg>
              {{numberFormat document.wordCount}} words
            </div>
            {{/if}}
          </div>
          
          {{#if document.metadata.tags}}
          <div class="article-tags">
            {{#each document.metadata.tags}}
            <a href="{{../site.baseUrl}}tags/{{slugify this}}/" class="tag">{{this}}</a>
            {{/each}}
          </div>
          {{/if}}
        </header>

        <!-- Knowledge Graph Preview -->
        {{#if options.includeKnowledgeGraph}}
        {{#if site.knowledgeGraph}}
        <div class="knowledge-graph-preview">
          <div class="graph-preview-title">
            Knowledge Connections
            <a href="{{site.baseUrl}}graph/" class="view-full-graph">View Full Graph →</a>
          </div>
          <div class="mini-graph" id="mini-graph">
            <!-- D3.js visualization will be rendered here -->
          </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- Article Content -->
        <article class="article-content">
          {{{document.content}}}
        </article>

        <!-- Related Content -->
        {{#if document.related}}
        <section class="related-content">
          <h2 class="related-title">Related Pages</h2>
          <div class="related-grid">
            {{#each document.related}}
            <div class="related-card">
              <h3><a href="{{path}}.html">{{title}}</a></h3>
              {{#if description}}
              <p class="related-description">{{description}}</p>
              {{/if}}
              {{#if tags}}
              <div class="related-tags">
                {{#each (take tags 3)}}
                <span class="tag">{{this}}</span>
                {{/each}}
              </div>
              {{/if}}
            </div>
            {{/each}}
          </div>
        </section>
        {{/if}}

        <!-- Backlinks -->
        {{#if options.includeBacklinks}}
        {{#if document.backlinks}}
        <section class="backlinks">
          <h2 class="backlinks-title">Referenced by {{document.backlinks.length}} pages</h2>
          <ul class="backlink-list">
            {{#each document.backlinks}}
            <li class="backlink-item">
              <div class="backlink-title">
                <a href="{{path}}.html">{{title}}</a>
              </div>
              {{#if context}}
              <div class="backlink-context">{{context}}</div>
              {{/if}}
            </li>
            {{/each}}
          </ul>
        </section>
        {{/if}}
        {{/if}}
      </div>

      <!-- Table of Contents Sidebar -->
      {{#if options.tableOfContents}}
      {{#if document.headings}}
      <aside class="table-of-contents">
        <h2 class="toc-title">Contents</h2>
        <nav class="toc-nav">
          <ul class="toc-list">
            {{#each document.headings}}
            <li class="toc-item toc-level-{{level}}">
              <a href="#{{anchor}}">{{text}}</a>
            </li>
            {{/each}}
          </ul>
        </nav>
      </aside>
      {{/if}}
      {{/if}}
    </div>
  </main>

  <!-- Search Modal -->
  {{#if options.generateSearch}}
  <div class="search-modal" id="search-modal">
    <div class="search-modal-content">
      <div class="search-header">
        <input type="search" class="search-input" id="search-input" placeholder="Search the knowledge base..." autocomplete="off">
        <button class="search-close" id="search-close">
          <svg width="24" height="24" fill="currentColor" viewBox="0 0 24 24">
            <path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/>
          </svg>
        </button>
      </div>
      <div class="search-results" id="search-results">
        <div class="search-instruction">Start typing to search...</div>
      </div>
    </div>
  </div>
  {{/if}}

  <!-- Scripts -->
  <script src="{{site.baseUrl}}assets/js/main.js"></script>
  
  {{#if options.includeKnowledgeGraph}}
  <script>
    // Mini knowledge graph visualization
    document.addEventListener('DOMContentLoaded', function() {
      const graphData = {{{json site.knowledgeGraph}}};
      if (graphData && graphData.nodes) {
        renderMiniGraph('#mini-graph', graphData);
      }
    });
    
    function renderMiniGraph(selector, data) {
      const container = d3.select(selector);
      const width = 280;
      const height = 180;
      
      const svg = container.append('svg')
        .attr('width', width)
        .attr('height', height);
      
      const simulation = d3.forceSimulation(data.nodes)
        .force('link', d3.forceLink(data.edges).id(d => d.id).distance(30))
        .force('charge', d3.forceManyBody().strength(-50))
        .force('center', d3.forceCenter(width / 2, height / 2));
      
      const link = svg.selectAll('.link')
        .data(data.edges)
        .enter().append('line')
        .attr('class', 'link')
        .attr('stroke', d => d.color)
        .attr('stroke-width', 1)
        .attr('opacity', 0.6);
      
      const node = svg.selectAll('.node')
        .data(data.nodes)
        .enter().append('circle')
        .attr('class', 'node')
        .attr('r', d => Math.min(d.size / 3, 8))
        .attr('fill', d => d.color)
        .attr('opacity', 0.8);
      
      node.append('title').text(d => d.label);
      
      simulation.on('tick', () => {
        link
          .attr('x1', d => d.source.x)
          .attr('y1', d => d.source.y)
          .attr('x2', d => d.target.x)
          .attr('y2', d => d.target.y);
        
        node
          .attr('cx', d => Math.max(8, Math.min(width - 8, d.x)))
          .attr('cy', d => Math.max(8, Math.min(height - 8, d.y)));
      });
    }
  </script>
  {{/if}}
</body>
</html>